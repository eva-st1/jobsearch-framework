# Fully local runtime. The git-ignored .local/local-mode sentinel enables this automatically.
JOBSEARCH_LOCAL_MODE="true"
JOBSEARCH_LOCAL_DATABASE_URL="postgresql:///jobsearch"

# Private, content-addressed storage for exact HTML/PDF renders. Keep this in your Mac backup.
JOBSEARCH_ARTIFACT_STORE=".local/artifacts"

PORT="8787"
